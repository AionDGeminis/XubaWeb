import { SoloDecimalDirective } from './solo-decimal.directive';

describe('SoloDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
